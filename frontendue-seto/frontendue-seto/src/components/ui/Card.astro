---
/**
 * OWNER: STATIC (Dev A)
 * Reusable Card component.
 */
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={["bg-white border border-gray-200 rounded-lg shadow-sm overflow-hidden hover:shadow-md transition-shadow", className]}>
  <slot />
</div>
